fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
transformers>=4.30.0
# torch is purposely not pinned to a specific version here to allow 
# flexible installation (CPU vs GPU) in different environments.
torch>=2.0.0
gunicorn>=21.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
tqdm>=4.65.0
